// var request = require("request");
var http = require("http");

var requestOptions = {
  host: "example.com",
  path: "/"
};

function readHTML (url, callback) {
  if (err) {
    throw err;
  }

console.log("The URL you sought is: ", response.HTTP);

};